{
  "name": "claude-quartermaster",
  "version": "0.1.0",
  "description": "Audit and explain which Claude Code extensions load in which projects, and what they cost.",
  "license": "MIT",
  "author": "Dean Hicks",
  "type": "module",
  "bin": {
    "qm": "./dist/cli.js"
  },
  "engines": {
    "node": ">=22.6"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "qm": "node --experimental-strip-types src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
